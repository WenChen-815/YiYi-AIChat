<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:fitsSystemWindows="true"
    tools:context=".ui.CharacterEditActivity">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="16dp">

        <!-- 角色名称输入 -->
        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="8dp"
            android:text="角色名称"
            android:textSize="16sp" />

        <EditText
            android:id="@+id/et_character_name"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:hint="请输入角色名称"
            android:inputType="textPersonName"
            android:maxLines="1" />

        <!-- 提示词输入 -->
        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="16dp"
            android:text="提示词"
            android:textSize="16sp" />

        <EditText
            android:id="@+id/et_prompt"
            android:layout_width="match_parent"
            android:layout_height="200dp"
            android:gravity="top"
            android:hint="请输入角色提示词（描述角色的性格、行为等）"
            android:inputType="textMultiLine" />

        <!-- 角色图片路径 (简化版，实际项目可替换为图片选择器) -->
        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="16dp"
            android:text="角色图片路径"
            android:textSize="16sp" />

        <Button
            android:id="@+id/btn_avatar_path"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="可选：角色头像图片路径" />

        <!-- 聊天背景路径 -->
        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="16dp"
            android:text="聊天背景路径"
            android:textSize="16sp" />

        <Button
            android:id="@+id/btn_background_path"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="可选：聊天背景图片路径" />

        <!-- 保存按钮 -->
        <Button
            android:id="@+id/btn_save"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="32dp"
            android:backgroundTint="?attr/colorPrimary"
            android:text="保存角色"
            android:textColor="@android:color/black" />
        <!-- 取消按钮 -->
        <Button
            android:id="@+id/btn_cancel"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="32dp"
            android:backgroundTint="?attr/colorPrimary"
            android:text="取消编辑"
            android:textColor="@android:color/black" />
    </LinearLayout>

</ScrollView>
